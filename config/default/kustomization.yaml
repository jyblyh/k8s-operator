# 总入口：make deploy 渲染这一份。
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: vntopo-system

resources:
  - ../namespace
  - ../crd
  - ../rbac
  - ../manager
  - ../agent

# 后续 M2/M3 加入 webhook 时，在这里追加：
# - ../webhook
